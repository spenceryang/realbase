{
  "buildCommand": "cd .. && npm install -g pnpm && pnpm install && cd web && pnpm run build",
  "outputDirectory": ".next",
  "framework": "nextjs",
  "installCommand": "echo 'handled in buildCommand'"
}
